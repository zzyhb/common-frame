server:
  port: 8081  #服务端口号

spring:
  application:
    name: cms #服务名称--调用的时候根据名称来调用该服务的方法
  datasource:
    url: jdbc:mysql://localhost:3306/ifugle?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 147258369

eureka:
  client:
    serviceUrl: #注册中心的注册地址
      defaultZone: http://127.0.0.1:8761/eureka/

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    auto-mapping-behavior: partial
    map-underscore-to-camel-case: true
# aliyun oss
aliyun:
  file:
    endpoint: https://oss-cn-hangzhou.aliyuncs.com
    accessKeyId: LTAI4Fydd4aPR89HRfDaYTBp
    accessKeySecret: gVyotP7q5dzQSmZo7gtVVVfN25QjMM
    bucketName: oss-yhb
    folder : ifugle
    webUrl: https://oss-yhb.oss-cn-hangzhou.aliyuncs.com